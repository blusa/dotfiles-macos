# SSH config — flota de Pablo (versionado en dotfiles-macos/ssh)
# Redes: Tailscale (100.x) y ZeroTier (10.147.18.x, *.odinedge.xyz)
# La llave privada NO se versiona: generar por máquina y agregar la .pub
# al authorized_keys de cada host.

# ── Servers / workstations (Tailscale) ───────────────────
Host zorak
    # Unraid NAS — SSH suele estar deshabilitado; usar web UI si falla
    HostName 100.78.112.90
    User root

Host blusa.cloud
    # Dokploy PaaS (VM en Zorak) — apps en *.blusa.cloud
    HostName 100.95.237.71

Host hermes
    # VM en Zorak, en setup — sin key auth todavía (pendiente)
    HostName 100.80.176.126

Host taz
    # Workstation backend/mobile (VM en Zorak) — POR DECOMISIONAR
    HostName 100.74.44.101

Host lola
    # Deep-learning box (Debian). Fallback: GLKVM web UI en http://100.94.141.50
    HostName 100.89.137.81

Host buster
    # MacBook Pro de Pablo (máquina local habitual)
    HostName 100.77.231.63

# ── Sensores Noctua (ZeroTier) ───────────────────────────
Host riki riki.odinedge.xyz
    # Dado por perdido (2026-09-03) — offline desde mayo
    HostName 10.147.18.105

Host mama mama.odinedge.xyz
    # No producción — tocable
    HostName 10.147.18.239

Host devbox devbox.odinedge.xyz
    # Sensor de desarrollo
    HostName 10.147.18.235
    User dior

Host nuno nuno.odinedge.xyz
    # RK3588, variante camara RGB-IR (baby monitor) — aun no deployado
    HostName 10.147.18.101

# ── Defaults (AL FINAL: en ssh config el primer valor gana, así que
#    los Host específicos de arriba deben poder pisar estos) ──────
Host *
    User blusa
    ServerAliveInterval 60
    ServerAliveCountMax 3
